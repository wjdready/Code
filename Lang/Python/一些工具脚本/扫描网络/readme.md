

```sh
# 扫描所有
# -sn：仅进行主机发现，跳过端口扫描，加快扫描速度
# -R：强制进行反向 DNS 解析，即根据 IP 地址查找对应的主机名
nmap -sn -R 192.168.0.0/24

# 快速更快的扫描 -n 不解析DNS
nmap -sn -n 192.168.0.0/24

# 扫描单个， 获取详细信息
nmap -sS -R -T4 -A 192.168.0.233

# 扫描所有 65535 个端口
nmap -sS -p- 192.168.0.244
```
