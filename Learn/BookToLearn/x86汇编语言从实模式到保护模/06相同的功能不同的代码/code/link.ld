
SECTIONS
{
   /* 已知程序会被加载到 0x7c00 处,这里将代码区便宜到 0x6c00, 
     这样才能和 elf 文件中地址对应, 并用于调试。
     至于在源码中添加 .org 0x7c00 似乎是和这个作用是一样的。
     但在实验中发现, 在源码中添加后整个程序编译起来会很大， .org 0x7c00 和这里的
     . = 0x7c00 意味着此处的地址, 而as源码中若定义此处地址 0x7c00 则编译器会将前面内容全部填充
     但在链接器中定义并不会. 总之 . = 0x7c00; 将会影响 接下来.text 中代码中的标签实际位置, 每匹配一个新的代码区,
     其标签都会在当前地址的基础上累加
    */
    . = 0x7c00;
    .text : {
        *(.text)
    }
}
